let x; let y; let velocidade = 5; function setup() {   createCanvas(500,300);   x = width/2   y = height/2 }  function draw() {   background(0)   circle(200,100,60)   fill(random(200),random(200),random(200))   stroke("lime")      rect(x,y,50,10)   fill("lightblue")   stroke("orange")       //movimento do rect  if (keyIsDown(RIGHT_ARROW)){     x += velocidade    }  if (keyIsDown(LEFT_ARROW)){    x -= velocidade   }    if (keyIsDown(UP_ARROW)){    y -= velocidade   }      if (keyIsDown(DOWN_ARROW)){    y +=velocidade   }        }
